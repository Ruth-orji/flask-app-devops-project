apiVersion: v1
kind: Service

metadata:
  name: flask-time-service

spec:
  selector:
    app: flask-time-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

  type: {{ .Values.service.type }}